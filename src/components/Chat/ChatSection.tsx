import { useState } from "react";
import { CustomClasses, ChannelList, Chat } from "stream-chat-react";
import ChannelContext from "../../context/channelStateContext";
import useAuthConnectUser from "../../hooks/useAuthConnectUser";
import ChannelContainer from "../Channel/ChannelContainer";

function ChatSection() {
  return (
    //add box shadow on this div
    <div>ChatSection</div>
  );
}

export default ChatSection;
